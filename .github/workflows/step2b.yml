name: Step2b

on:
  workflow_call:
    inputs:
      image_name:
        description: "test"
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
